---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
	(a, b) => {
		const dateA = a.data.date ? new Date(a.data.date).valueOf() : 0;
		const dateB = b.data.date ? new Date(b.data.date).valueOf() : 0;
		return dateB - dateA;
	}
);

const colors = ['bg-brutalist-yellow', 'bg-brutalist-pink', 'bg-brutalist-blue', 'bg-brutalist-green', 'bg-brutalist-orange'];
---

<Layout>
	<!-- Hero Section -->
	<section class="border-b-3 border-brutalist-black bg-brutalist-yellow">
		<div class="max-w-5xl mx-auto px-6 py-16 md:py-24">
			<h1 class="text-5xl md:text-7xl font-bold leading-tight mb-6 text-brutalist-black">
				THOUGHTS.<br/>
				CODE.<br/>
				<span class="inline-block bg-brutalist-black text-brutalist-white px-4 py-1">DESIGN.</span>
			</h1>
			<p class="text-xl md:text-2xl max-w-2xl text-brutalist-black">
				A neobrutalist blog where I write about web development, design philosophy, and the occasional random thought.
			</p>
		</div>
	</section>

	<!-- Blog Posts -->
	<section class="max-w-5xl mx-auto px-6 py-16">
		<h2 class="text-3xl font-bold mb-12 flex items-center gap-4">
			<span class="inline-block w-8 h-1 bg-brutalist-black"></span>
			LATEST POSTS
		</h2>

		<div class="grid gap-8">
			{posts.map((post, index) => (
				<article class="brutal-card group">
					<div class="flex flex-col md:flex-row gap-6">
						<div class={`w-full md:w-32 h-32 md:h-auto ${colors[index % colors.length]} brutal-border flex items-center justify-center shrink-0`}>
							<span class="text-4xl font-bold text-brutalist-black">{String(index + 1).padStart(2, '0')}</span>
						</div>
						<div class="flex-1">
							<h3 class="text-2xl md:text-3xl font-bold mb-3 group-hover:text-brutalist-pink transition-colors">
								<a href={`/${post.slug}`} class="hover:underline decoration-3 underline-offset-4">
									{post.data.title}
								</a>
							</h3>
							{post.data.description && (
								<p class="text-lg opacity-80 mb-4">
									{post.data.description}
								</p>
							)}
							{post.data.date && (
								<time class="font-mono text-sm">
									{new Date(post.data.date).toLocaleDateString('en-US', {
										year: 'numeric',
										month: 'long',
										day: 'numeric'
									})}
								</time>
							)}
						</div>
					</div>
				</article>
			))}
		</div>
	</section>
</Layout>
